<?xml version="1.0" encoding="UTF-8"?>

    <androidx.constraintlayout.widget.ConstraintLayout tools:context=".sms_app" android:layout_height="match_parent" android:layout_width="match_parent" xmlns:tools="http://schemas.android.com/tools" xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">

    <EditText
        android:layout_height="60dp"
        android:layout_width="290dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/border"
        android:paddingLeft="15dp"
        android:inputType="number"
        android:hint="Phone Number"
        android:layout_marginTop="250dp"
        android:id="@+id/number"/>

    <EditText
        android:layout_height="60dp"
        android:layout_width="290dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/border"
        android:inputType="textMultiLine"
        android:hint="Message"
        android:layout_marginTop="50dp"
        android:id="@+id/message"
        app:layout_constraintTop_toBottomOf="@+id/number"
        android:padding="15dp"/>

    <Button
        android:layout_height="60dp"
        android:layout_width="290dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/border"
        android:layout_marginTop="50dp"
        android:id="@+id/send"
        app:layout_constraintTop_toBottomOf="@+id/message"
        android:textStyle="bold"
        android:text="Send Sms"/>
</androidx.constraintlayout.widget.ConstraintLayout>